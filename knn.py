class Knn:
	# list_features = [ features from 1st stream, features from 2nd stream , etc... ]
	# should return id or None, case none the system will assign new id
	def predict(list_features):
		return None

	# list_labels_features = [ [<id>, features], [<id>, features], ...etc ]
	def learn(list_labels_features):
		pass